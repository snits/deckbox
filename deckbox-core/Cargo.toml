[package]
name = "deckbox-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
rand = "0.8"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
